node_modules/
dist/
.wrangler/
.next/
.open-next/
.env
.env.local
.env.*.local
.dev.vars
.dev.vars.*
!.dev.vars.example
*.log
*.tsbuildinfo
worker-configuration.d.ts
.DS_Store
coverage/
.turbo/
playwright-report/
test-results/
blob-report/

# Next.js generated types
apps/web/next-env.d.ts

# git bundles -- delivery artifacts from the Continuing Development
# workflow (each approved block ships as a *.bundle at the repo root
# alongside its commit); intentionally untracked, not repository hygiene
# debt.
*.bundle
